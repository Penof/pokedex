import { PokemonInterceptor } from './pokemon-interceptor';

describe('PokemonInterceptor', () => {
  it('should create an instance', () => {
    expect(new PokemonInterceptor()).toBeTruthy();
  });
});
